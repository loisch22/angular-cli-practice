//all routes should be named app.routing.ts

//pkg from Angular helps provide the router to the rest of the application
import { ModuleWithProviders } from '@angular/core';

//render specific components when specific URL are provided to the router
import { Routes, RouterModule } from '@angular/router';
